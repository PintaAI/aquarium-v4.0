import { handlers } from "@/auth"

export const { GET, POST } = handlers

// This route handler is used by NextAuth.js for authentication
// It handles all authentication-related requests under /api/auth/*
